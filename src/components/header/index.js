import React from 'react';

import './styles.css'

const Header = () => (
    <header id="main-header">
        <div>
            <h1>Entrar em uma sala</h1>
        </div>
    </header>
);

export default Header;